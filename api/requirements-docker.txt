# Container deps for the AWS (CPU) path. Pinned to the versions in the training
# .venv so behaviour matches, but with the CPU-only torch wheels (the GPU build
# is ~5 GB and useless on a CPU EC2 instance).
#
# CPU torch/torchvision come from PyTorch's CPU index (see Dockerfile --index-url).
torch==2.11.0
torchvision==0.26.0

# Model + preprocessing stack (same versions as .venv)
segmentation-models-pytorch==0.5.0
timm==1.0.27
albumentations==1.4.24
opencv-python-headless==4.13.0.92
numpy==2.4.4
pillow==12.2.0
scipy==1.17.1
safetensors==0.8.0
PyYAML==6.0.3

# API server
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
